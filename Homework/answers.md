1. The command to set up a git repository is git init
2. The command used to track changes is git add
3. The command we use to get updates is git fetch
4. The command to add to repository is git push
